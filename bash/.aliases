# Git aliases
alias gc='git commit -m'
alias gi='git init'
alias ga='git add'
alias gp='git push'
alias gpo='git push -u origin main'
alias grao='git remote add origin'
alias st='git status'
alias cm='git commit -m'
alias xb='git branch -d'
alias mb='git checkout main'
alias sb='git checkout'
alias nb='git checkout -b'
alias lb='git branch'
alias upc='git reset --soft HEAD~1'
alias clone='git clone'

# Navigation aliases
alias rt='echo "Navigated into the root directory."; cd ~'
alias dfi='echo "Navigated into the dotfiles directory."; cd ~/developer/dotfiles'
alias nvc='echo "Navigated into the neovim config directory."; cd ~/.config/nvim'
alias dv='echo "Navigated into the developer directory."; cd ~/developer'
alias rfi='echo "Navigated into the root directory."; cd ~'
alias root='echo "Navigated into the root directory."; cd ~'

# File aliases
alias rmd='rm -rf'
alias mk='mkdir'
alias ls='echo; gls -a --color=auto; echo'
alias .a='echo "Sourced aliases."; source ~/.aliases'
alias .z='echo "Sourced .bashrc."; source ~/.bashrc'
alias .b='echo "Sourced .bashrc."; source ~/.bashrc'

mkcd() {
    mkdir -p "$1"
    cd "$1"
}

cpb() {
    cat "$1" | pbcopy
    echo 'Copied the contents of' $1 'to clipboard.'
}

# Neovim aliases
alias nv='nvim'
alias ea='nvim ~/.aliases'
alias ez='nvim ~/.bashrc'
alias eb='nvim ~/.bashrc'

# Other
alias cl='clear'
alias hc='echo "Terminal history cleared"; history -c'
